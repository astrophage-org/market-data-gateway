syntax = "proto3";
package nte.marketdata;

option java_package = "com.gfmg.nte.marketdata";
option go_package = "github.com/apex/market-data-gateway/protos";

// Consumed from nte.orderbook.snapshots topic published by order-matching-engine
message OrderBookSnapshot {
    string symbol = 1;
    repeated Level bids = 2;
    repeated Level asks = 3;
    int64 timestamp_ns = 4;
}

message Level {
    double price = 1;
    double quantity = 2;
    int32 order_count = 3;
}

// Consumed from nte.trades.matched topic published by order-matching-engine
message TradeTick {
    string trade_id = 1;
    string symbol = 2;
    double price = 3;
    double quantity = 4;
    int64 matched_at_ns = 5;
    string taker_side = 6;
}
